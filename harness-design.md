# Loom Harness Design

## 1. 文档定位

本文档定义 Loom 的完整 harness 方案。

它回答的是：

- 一个仓库如何被接入 Loom
- 执行过程中上下文如何绑定
- 工作现场如何建立和隔离
- 多轮事项如何 checkpoint、resume、handoff
- 当前状态如何被读取
- 哪些问题应被脚本前置发现
- merge gate 在执行侧如何运行

本文档不负责事项分类、规格准入和成熟度定义；这些属于 `governance-design.md`。

## 2. Harness 目标

Loom 的 harness 方案要同时满足六个目标：

1. 让执行上下文不依赖会话记忆
2. 让每个正式事项有清晰工作现场
3. 让多轮事项可以暂停、恢复和交接
4. 让状态可被快速读取，而不制造第二真相源
5. 让适合自动化的判断尽量前置
6. 让 merge gate 只承担执行放行，而不是承担全部治理

## 3. 初始化机制

Harness 的第一步不是运行，而是初始化。

初始化机制负责回答：

- 当前仓库是哪种场景
- 应装哪些 Loom 组件
- 应生成哪些工件
- 应建立哪些初始事项和检查点

Loom 默认支持三种初始化场景：

- 新项目初始化
- 小型既有仓库接入
- 复杂既有仓库接入

初始化机制的目标不是一次性装齐全部能力，而是用正确强度建立执行基础。

### 3.1 初始化产物

一个完整的初始化机制，不应只输出“建议装什么”，还应输出初始化产物。

Loom 目标上至少应支持以下产物：

- 初始化脚本或初始化步骤入口
- 初始能力清单
- 首批事项清单
- 初始 checkpoint / progress 载体
- 初始 clean state

### 3.2 初始化后的 clean state

初始化完成后，目标仓库应处于清晰起点，而不是半装配、半手动的模糊状态。

这意味着：

- 基础文件已经落位
- 首批事项已经建立
- 后续执行有明确入口
- 执行前状态可被恢复复用

## 4. 执行上下文机制

每轮正式执行都必须绑定执行上下文。

执行上下文至少要能回答：

- 当前事项是谁
- 当前事项属于哪条路径
- 当前目标是什么
- 当前工作现场是什么
- 当前恢复入口是什么
- 当前处于哪个 checkpoint

Loom 固定的是语义集合，不固定字段命名。

执行上下文的目标，是让执行、恢复、review 和 handoff 都建立在同一组最小事实之上。

### 4.1 每轮读取

完整 harness 不只定义上下文字段，还定义每轮执行前应该读取什么。

Loom 目标上应支持：

- 先读当前 progress / checkpoint
- 先读最近 git 历史
- 再决定当前轮次只推进哪个清晰单元

## 5. 工作现场机制

Harness 默认要求正式事项有明确工作现场。

### 5.1 基本原则

- 单现场单事项
- 一个正式现场只服务一个主要目标
- 不在脏现场继续叠加正式事项

### 5.2 现场定位

工作现场必须可恢复定位。

这意味着：

- 现场应能由稳定输入推导
- 同一事项的恢复动作应能回到同一现场
- 现场命名可以参数化，但不能完全依赖人脑记忆

### 5.3 现场强度

对于不同仓库，现场机制可以有不同强度：

- 轻量形态
  - 只要求清晰现场和单事项原则
- 标准形态
  - 明确隔离现场与恢复定位
- 强化形态
  - 引入更严格的现场生命周期和纯度预检

## 6. Checkpoint / Resume / Handoff 机制

Harness 的核心，不是“记录很多”，而是“留下正确恢复入口”。

### 6.1 Checkpoint

Checkpoint 的目标是记录当前停点。

一个有效 checkpoint 至少要包含：

- 当前做到了哪里
- 已验证了什么
- 还有什么阻断项
- 下一步是什么

### 6.2 Resume

Resume 的目标是让后续执行者不重新侦察整仓。

Loom 默认要求：

- 每个正式事项有唯一恢复主入口
- 任何继续执行动作，都优先回到这个入口

### 6.3 Handoff

Handoff 的目标是让工作可以在不同人或不同 agent 之间平滑转移。

一个最小 handoff 至少应表达：

- 当前状态
- 当前停点
- 下一步
- 风险
- 所需上下文入口

### 6.4 轻量与标准形态

Loom 默认承认两种恢复形态：

- `checkpoint-lite`
  - 停点、下一步、阻断项寄存在 issue / PR 描述中
- 标准恢复形态
  - 使用明确恢复工件和唯一恢复主入口

何时从轻量升级到标准，取决于：

- 事项是否经常跨多轮推进
- 中断恢复成本是否明显升高
- 是否已经出现多入口并行记录的问题

### 6.5 每轮回写

完整 harness 不只要求能恢复，还要求每轮结束后回写执行结果。

Loom 目标上应支持：

- 更新 progress / checkpoint
- 更新下一步
- 记录已完成的验证
- 在需要时形成明确提交点

## 7. 状态机制

Harness 需要状态机制，但不需要第二套真相源。

### 7.1 状态的目标

状态机制用于快速回答：

- 当前事项是什么
- 当前处于哪个阶段
- 当前停点是什么
- 下一步是什么
- 当前阻断项是什么
- 最近验证了什么

### 7.2 状态来源

状态应尽量从已有真相源派生：

- issue
- PR
- checkpoint
- 恢复工件
- 验证记录

### 7.3 状态边界

状态面只能用于读取，不应用来制造一套新的平行真相。

### 7.4 运行时可见性

Harness 的状态能力不应只停留在文档状态。

Loom 目标上还应覆盖运行时可见性：

- 当前工作现场的运行入口
- 日志可见
- 指标可见
- trace 或等价诊断信息可见
- UI 或端到端结果可被 agent 直接验证

运行时可见性的目标，是让 agent 不只读文档，也能读运行事实。

## 8. 自动化前置机制

Harness 默认把适合自动化的判断前置到脚本或 CI。

### 8.1 应前置的问题

至少包括：

- 结构完整性
- 必填工件是否存在
- 明显纯度问题
- 明显范围越界信号
- 基础状态不一致

### 8.2 不应前置的问题

以下问题不应被假装成脚本能完全解决：

- 方向是否正确
- 方案是否合理
- 风险是否真的收口
- 是否值得做

Harness 的原则是：

- 机械判断交给机器
- 语义判断保留给人或 reviewer

### 8.3 机械化校验范围

Loom 的自动化前置不应只覆盖代码和 PR。

目标上还应覆盖：

- 知识库结构完整性
- 文档交叉链接
- 模板存在性
- 关键规则落点存在性
- 纯度与明显越界

## 9. Merge Gate 机制

Merge gate 是 harness 的执行放行层，不是治理替代品。

### 9.1 Merge gate 回答什么

- 当前 head 是否满足进入主干的执行质量线
- 当前执行材料是否完整
- 当前自动检查是否通过

### 9.2 Merge gate 不回答什么

- 这个事项是否值得做
- 这个方向是否第一次被认真判断
- 高风险边界是否应该现在才第一次讨论

### 9.3 与 governance 的关系

治理系统定义前置判断。
harness 系统保证这些判断对应的执行工件、检查和放行流程能运转。

### 9.4 单单元增量推进

Loom 目标上的长时运行 harness，应鼓励每轮只推进一个清晰单元，而不是在单轮执行中并行吞入过多目标。

这项能力的意义是：

- 降低上下文漂移
- 降低恢复难度
- 提高 progress 记录质量

## 10. Harness 强度模型

Loom 不把 harness 做成单一重型系统，而是做成可升级结构。

### 10.1 轻量形态

适用于：

- 小型仓库
- 低复杂度事项
- 恢复痛点还不明显的仓库

默认启用：

- 执行上下文最小集
- 单现场单事项
- checkpoint-lite
- 基础自动化前置

### 10.2 标准形态

适用于：

- 事项经常跨多轮推进
- 已经出现恢复成本
- 已经需要明确 handoff

默认启用：

- 标准恢复入口
- 明确 checkpoint / resume / handoff
- 更强状态读取

### 10.3 强化形态

适用于：

- 复杂仓库
- 高治理成本
- 多 agent 并行
- 高恢复成本

默认启用：

- 更严格的现场规则
- 更强状态面
- 更强纯度检查
- 更强自动化前置

## 11. Harness 方案的一句话总结

Loom 的 harness 方案不是为了制造更多执行仪式，而是为了：

- 让执行有上下文
- 让现场可定位
- 让多轮事项可恢复
- 让状态可读取
- 让机械问题尽量在 review 前被发现
