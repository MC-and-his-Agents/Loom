# Loom Harness Design

## 1. 文档定位

本文档定义 Loom 的完整 harness 方案。

它只负责三类内容：

- 定义 harness 需要覆盖的完整能力面
- 定义各稳定组件如何组合成执行闭环
- 定义尚未拆成独立组件的方案级能力

`harness/*.md` 是稳定组件文档，负责承接最小可执行规则。

当某项能力已经有稳定组件时，本文档只说明它在完整方案中的职责与边界，不重复字段级规则、最小事实集或脚本细节。

本文档不负责事项分类、规格准入和成熟度定义；这些属于 `governance-design.md`。

## 2. 方案与组件边界

| 主题 | 稳定落点 | 本文档职责 |
| --- | --- | --- |
| 初始化与装配 | `harness-design.md` | 定义初始化场景、初始化产物、初始 clean state |
| work item / exec-plan | `harness/work-item-contract.md` | 定义正式执行单元；本文只定义它在执行闭环中的位置 |
| 执行上下文 | `harness/execution-context.md` | 定义上下文合同；本文只要求每轮正式执行必须绑定 |
| 工作现场 | `harness/workspace-model.md` | 定义隔离现场与恢复定位；本文只定义它与恢复和纯度的组合关系 |
| 纯度预检 | `harness/workspace-and-purity.md` | 定义纯度规则；本文只定义它在正式执行前的作用 |
| 恢复模型 | `harness/recovery-model.md` | 定义 `checkpoint`、`resume`、`handoff`；本文只定义它们在每轮执行中的使用顺序 |
| 状态面与运行事实 | `harness/status-surface.md` + `harness-design.md` | `status-surface.md` 固化最小状态输出；本文补充运行时可见性是方案级目标 |
| 自动化前置 | `harness/automation-frontload.md` + `harness-design.md` | `automation-frontload.md` 固化前置边界；本文补充知识结构、模板和执行支撑也在前置范围内 |
| merge gate | `harness-design.md` | 定义它与 governance、自动化检查和执行放行的关系 |

## 3. Harness 目标

Loom 的 harness 方案要同时满足六个目标：

1. 让执行上下文不依赖会话记忆
2. 让每个正式事项有清晰工作现场
3. 让多轮事项可以暂停、恢复和交接
4. 让状态可被快速读取，而不制造第二真相源
5. 让适合自动化的判断尽量前置
6. 让 merge gate 只承担执行放行，而不是承担全部治理

## 4. 初始化与装配

Harness 的第一步不是运行，而是初始化。

初始化机制负责回答：

- 当前仓库是哪种接入场景
- 应启用哪些 harness 组件
- 应生成哪些初始工件
- 后续执行从哪里进入

Loom 默认支持三种初始化场景：

- 新项目初始化
- 小型既有仓库接入
- 复杂既有仓库接入

初始化的目标不是一次性装齐全部能力，而是用合适强度建立执行基础。

初始化至少应产出以下结果：

- 初始化脚本或等价入口
- 初始能力清单
- 首批事项清单
- 初始 checkpoint / progress 载体
- 初始 clean state

初始化完成后，目标仓库应处于清晰起点，而不是半装配、半手动的模糊状态。

这意味着：

- 基础文件已经落位
- 首批事项已经建立
- 后续执行有明确入口
- 执行前状态可被恢复复用

## 5. 跨组件执行闭环

完整 harness 不是若干孤立规则，而是一条稳定执行闭环。

### 5.1 正式事项进入执行

正式事项进入 harness 时，必须先有 work item 或等价执行单元。

它负责把当前目标、路径、范围和关联工件压成可执行入口。

### 5.2 每轮进入前绑定与读取

每轮正式执行开始前，至少应完成以下动作：

- 绑定执行上下文
- 回到唯一恢复主入口
- 读取当前 progress / checkpoint
- 读取最近 git 历史
- 决定本轮只推进一个清晰单元

这一步由 `execution-context`、`work-item-contract` 和 `recovery-model` 共同支撑。

### 5.3 在隔离现场推进

正式执行默认在可恢复定位的隔离现场中推进。

`workspace-model` 负责保证现场与事项的映射关系，`workspace-and-purity` 负责阻止在脏现场中继续叠加无关目标。

### 5.4 每轮结束回写与恢复

每轮执行结束后，必须能把结果回写到恢复体系中，而不是只留在聊天记录或本地记忆里。

最小回写目标包括：

- 当前停点
- 下一步
- 已验证事实
- 当前阻断项
- 在需要时形成明确提交点

### 5.5 状态读取与运行时可见性

状态面只服务读取，不服务并行记账。

完整 harness 还要求 agent 能读取运行事实，而不只读取文档事实。

方案级最小目标包括：

- 当前工作现场的运行入口可见
- 日志可见
- 指标可见
- trace 或等价诊断信息可见
- UI 或端到端结果可被 agent 直接验证

`status-surface.md` 固化状态面的最小输出语义；运行时可见性则作为完整 harness 的能力目标，由具体仓库按自身技术栈实现。

## 6. 自动化前置与机械化校验

Harness 默认把适合自动化的判断前置到脚本或 CI。

至少包括：

- 结构完整性
- 必填工件存在性
- 明显纯度问题
- 明显范围越界信号
- 基础状态不一致
- 知识库结构完整性
- 文档交叉链接
- 模板存在性
- 关键规则落点存在性

不应被错误前置的问题包括：

- 方向是否正确
- 方案是否合理
- 风险是否真的收口
- 是否值得做

Harness 的原则是：

- 机械判断交给机器
- 语义判断保留给人或 reviewer

## 7. Merge Gate 边界

Merge gate 是 harness 的执行放行层，不是治理替代品。

它回答的是：

- 当前 head 是否满足进入主干的执行质量线
- 当前执行材料是否完整
- 当前自动检查是否通过

它不回答的是：

- 这个事项是否值得做
- 这个方向是否第一次被认真判断
- 高风险边界是否应该现在才第一次讨论

治理系统定义前置判断；harness 系统保证这些判断对应的执行工件、检查和放行流程能运转。

## 8. Harness 强度模型

Loom 不把 harness 做成单一重型系统，而是做成可升级结构。

强度变化通过启用不同组件组合与更强约束完成，而不是通过重复定义另一套规则完成。

### 8.1 轻量形态

适用于：

- 小型仓库
- 低复杂度事项
- 恢复痛点还不明显的仓库

默认启用：

- 执行上下文最小集
- 单现场单事项
- `checkpoint-lite`
- 基础自动化前置

### 8.2 标准形态

适用于：

- 事项经常跨多轮推进
- 已经出现恢复成本
- 已经需要明确 handoff

默认启用：

- 标准恢复入口
- 明确 `checkpoint` / `resume` / `handoff`
- 更强状态读取

### 8.3 强化形态

适用于：

- 复杂仓库
- 多 agent 并行
- 高恢复成本
- 高治理成本

默认启用：

- 更严格的现场规则
- 更强状态面与运行时可见性
- 更强纯度检查
- 更强自动化前置

## 9. Harness 方案的一句话总结

Loom 的 harness 方案不是为了制造更多执行仪式，而是为了让初始化、执行、恢复、可见性和放行形成一条可升级、可组合、可验证的执行闭环。
