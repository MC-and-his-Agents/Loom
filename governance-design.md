# Loom Governance Design

## 1. 文档定位

本文档定义 Loom 的完整治理方案。

它回答的是：

- 一件事从提出到收口，应该经过哪些治理环节
- 不同类型事项分别走什么路径
- 在什么时点做什么判断
- 谁负责什么判断
- 什么叫进入下一阶段，什么叫真正完成

本文档不负责执行现场、恢复机制、状态桥接和脚本化支撑；这些属于 `harness-design.md`。

## 2. 治理目标

Loom 的治理方案要同时满足五个目标：

1. 让事项进入执行前先被正确分类
2. 让高影响改动在实现前就完成必要收口
3. 让中途偏航可以被及时发现，而不是拖到 merge 前
4. 让 merge 前判断只承担放行职责，而不是第一次理解事项
5. 让事项状态、文档状态、审查状态和主干状态最终一致

## 3. 事项模型

Loom 默认把事项分成三类：

- 轻量事项
- 中等事项
- 边界事项

这个分类不是按“文档多少”区分，而是按“影响范围和治理成本”区分。

### 3.1 轻量事项

轻量事项的典型特征是：

- 小型缺陷修复
- 测试补充
- 文案、说明、示例修订
- 不改变公共边界的局部重构

轻量事项默认可以直接进入实现。

轻量事项的前提是：

- 不改变共享契约
- 不改变核心抽象
- 不改变公共事件结构
- 不改变配置、CLI、运行模型或部署方式

### 3.2 中等事项

中等事项的典型特征是：

- 需要一定设计判断
- 会影响局部职责、局部接口或局部流程
- 还没有达到正式边界事项的治理强度

中等事项默认不要求完整正式规格，但要求在进入实现前至少有简化设计说明。

### 3.3 边界事项

边界事项的典型特征是：

- 改动共享契约
- 改动共享数据模型或公共事件结构
- 改动核心抽象或核心职责边界
- 改动配置模型、CLI 契约或运行方式
- 改动部署和守护方式

边界事项默认必须先说明再实现。

## 4. 事项入口机制

事项入口机制决定一件事如何从“想法”进入“执行承诺”。

Loom 的默认入口规则是：

1. 先判断事项类型
2. 再判断是否需要说明工件
3. 再判断是否具备进入实现的条件

这里最重要的约束是：

- 不允许把边界事项直接作为普通实现进入 PR
- 不允许因为流程怕重而取消对高影响事项的前置判断
- 不允许把所有事项一刀切进同一条流程

## 5. 规格准入机制

Loom 默认认为，“先说明再实现”不是理念口号，而是正式准入机制。

### 5.1 何时触发规格准入

以下任一成立时，默认触发规格准入：

- 共享契约变化
- 公共边界变化
- 运行模型变化
- 部署方式变化
- 高风险核心抽象变化

### 5.2 准入所需工件

正式边界事项默认至少需要：

- `spec.md`
- `plan.md`

中等事项可以只使用简化设计说明，不强制要求完整正式规格。

### 5.3 准入通过标准

一个事项只有在以下问题已收口后，才应进入实现：

- 为什么值得做
- 本次改什么，不改什么
- 影响哪些边界
- 如何判断实现达标
- 哪些风险已知，如何验证

## 6. 三个正式 checkpoint

Loom 的治理方案默认有三个正式判断点。

### 6.1 Commit checkpoint

这是事项进入正式执行前的判断点。

它回答：

- 这件事是什么类型
- 该走哪条路径
- 是否已经具备进入实现承诺的条件

Commit checkpoint 的目标不是审批一切，而是避免方向未收口就进入实现。

### 6.2 Build checkpoint

这是实现过程中的判断点。

它回答：

- 当前实现是否仍在已批准轨道上
- 是否出现了新的边界变化或新风险
- 是否需要回退到说明层重新讨论

Build checkpoint 的目标是中途纠偏，而不是等到快合并时再集中爆雷。

### 6.3 Merge checkpoint

这是合并前的判断点。

它回答：

- 当前 head 是否满足进入主干的质量线
- 当前实现是否忠实落地了前面已确认的方向

Merge checkpoint 不负责第一次高质量语义判断。

## 7. 审查职责分层

Loom 的治理方案默认把审查职责分成四类：

- 作者
- reviewer
- 自动检查
- merge gate

### 7.1 作者职责

作者负责：

- 正确判断事项路径
- 补齐所需工件
- 提供最小必要上下文
- 说明验证与风险

### 7.2 Reviewer 职责

Reviewer 负责：

- 判断方向是否正确
- 判断是否符合当前路径的准入要求
- 判断实现是否偏离边界
- 判断当前改动是否达到 merge-ready

### 7.3 自动检查职责

自动检查负责：

- 结构完整性
- 必填工件存在性
- 基础一致性
- 明显范围越界信号

自动检查不负责语义正确性。

### 7.4 Merge gate 职责

Merge gate 负责最终放行，不负责补做前序阶段判断。

## 8. 成熟度模型

Loom 的治理方案默认要求事项具有明确成熟度。

至少要区分：

- 说明已清楚
- 实现进行中
- 合并就绪
- 已进入主干并收口

成熟度模型的目标，是避免把说明完成写成实现完成，或把局部实现写成事项已结束。

## 9. 关闭语义

Loom 默认要求关闭语义与事项成熟度保持一致。

只有当以下状态一致时，事项才算真正完成：

- 事项目标已达成
- 必要实现已进入主干
- 验证结论已具备
- 文档与主干状态一致

不允许：

- 用“说明通过”替代“实现完成”
- 用“PR 已开”替代“事项完成”
- 用“局部结论成立”制造“整体已结束”的假象

## 10. 真相源模型

治理方案还要求不同载体承担不同真相。

### 10.1 GitHub 侧真相

GitHub 负责：

- 事项调度
- 状态管理
- 依赖关系
- 优先级
- 执行分解

### 10.2 仓库侧真相

仓库负责：

- 规则
- 规格
- 模板
- 执行机制
- 恢复与状态结构

### 10.3 仓库知识库模型

治理方案不仅要求“知识入库”，还要求知识以可持续维护的方式入库。

Loom 默认把仓库知识库设计成两层：

- 短入口文档
  - 例如短 `AGENTS.md`
  - 负责指路、权威来源和读取顺序
- 深知识文档
  - 负责承接规则、设计、模板、运行机制

不允许：

- 把所有知识压进一个超大根文档
- 让知识只存在于会话里
- 让规则散落在大量无法机械检查的重复片段中

### 10.4 载体职责矩阵

治理方案默认要求以下载体职责分离：

- Issue
  - 事项本体、目标、边界、关闭条件
- Project
  - 调度位置、优先级、阶段归属
- PR
  - 本次改动范围、验证与风险事实
- 规则文档
  - 长期稳定规则
- 规格文档
  - 边界事项的正式说明
- 执行工件
  - checkpoint、resume、handoff、状态桥接

### 10.5 载体职责分离

Issue、PR、规则文档、规格文档、执行工件不得承担同一份完整真相。

Loom 的默认原则是：

- 每条规则有唯一权威落点
- 其他载体只做引用、摘要或执行承接

## 11. 机械化治理能力

治理方案不仅定义规则，还要求这些规则逐步具备机械化校验能力。

至少应覆盖：

- 规则落点是否存在
- 核心文档结构是否完整
- 关键模板是否存在
- 交叉引用是否断裂
- 已明确的载体职责是否被明显破坏

这些检查不替代 reviewer，但它们应减少低价值的人肉重复劳动。

## 12. 治理方案的一句话总结

Loom 的治理方案不是让所有事项都变重，而是：

- 让轻量事项快速进入执行
- 让边界事项先收口再实现
- 让高价值判断发生在正确时点
- 让事项最终以一致状态收口
