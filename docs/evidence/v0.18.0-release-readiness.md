# v0.18.0 Release Readiness And Closeout Evidence

## Scope

- Work Item: `WI-1696`
- Issue: `#1696`
- Phase: `#1680`
- Milestone: `#15`
- Target release: `v0.18.0`
- npm package: `@mc-and-his-agents/loom@0.18.0`
- Branch: `work/1696-release-closeout`

This release publishes the completed intensity-aware ship path from milestone #15. It consumes governance intensity classification, PR metadata repair/readback, compressed gate diagnostics, `loom ship` dry-run/apply, closeout policy selection, controlled-merge closeout-run linkage, and README/skills/fixtures convergence.

This release does not introduce a new release workflow, a new plugin surface version, a deprecated installer release, or a new runtime installation model.

## Milestone 15 Readback

- #1681, #1685, #1689, and #1693 are closed parent FRs.
- #1682, #1683, #1684, #1686, #1687, #1688, #1690, #1691, #1692, #1694, and #1695 are closed Work Items.
- #1694 closed after PR #1708 merged and closeout check passed.
- #1696 is the active release closeout issue.
- #1680 remains open until #1696 release readback passes and milestone #15 has no other open issues.

## Candidate Occupancy

- 2026-06-22 pre-bump readback classified `v0.18.0` as `unpublished`:
  - npm `@mc-and-his-agents/loom@0.18.0`: missing; latest was `0.17.1`.
  - GitHub Release `v0.18.0`: missing.
  - git tag `v0.18.0`: missing.
  - matching successful `loom-cli-release` workflow for `v0.18.0`: missing.
- Current version authority in this PR:
  - `VERSION`: `v0.18.0`
  - `package.json`: `0.18.0`
  - Root package: `@mc-and-his-agents/loom`
  - Bin: `loom`

## Consumed Behavior

- `loom ship` is the primary delivery entry for ordinary light/standard implementation PRs.
- `loom ship --apply` consumes PR gate, hosted checks, controlled merge, and closeout policy.
- Closeout policy chooses inline/host-only closeout for ordinary eligible work, batched carrier closeout when versioned carrier sync is needed, and explicit closeout PRs for release, parent/milestone, multi-Work-Item, conflict, or reinforced scenarios.
- PR metadata can be safely rendered, read back, and repaired without requiring users to hand-edit duplicate fact fields.
- Gate diagnostics are compressed to actionable blockers and next actions while full diagnostics remain available through artifacts.
- README, README.zh-CN, source skills, generated skills, plugin skills, and targeted fixtures all point users to the `loom ship` main path.

## Pre-Merge Validation Commands

The release PR must run these at the PR head before review and PR gate:

- `CODEX_EXPORT_GH_TOKEN=1 PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py release readback --target . --version v0.18.0 --package @mc-and-his-agents/loom --repo MC-and-his-Agents/Loom --release-judgment release_required --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/version_surface_check.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_release_surface.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py`
- `npm run test:package`
- `npm pack --dry-run --json --ignore-scripts`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite validate --target . --item WI-1696 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite evidence validate --target . --item WI-1696 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite carrier validate --target . --item WI-1696 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_flow.py fact-chain --target . --item WI-1696`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_flow.py state-check --target . --item WI-1696`
- `git diff --check`

## Publish Boundary

Merging the release PR to `main` is publish-capable because this branch changes `VERSION` and root `package.json`. The expected publish mechanism is the existing `loom-cli-release` workflow triggered by push to `main`.

The publish action is authorized by milestone #15 closeout. The workflow must still fail closed if the version is already occupied, package metadata does not match `VERSION`, `NPM_TOKEN` is unavailable, or release evidence becomes partial.

## Post-Merge Release Closeout Contract

After the release PR merges, closeout must read back:

- Release PR number, PR head, merge commit, and merge time.
- `loom-cli-release` workflow run for the merge commit.
- Git tag `v0.18.0` and GitHub Release `v0.18.0`.
- npm `@mc-and-his-agents/loom@0.18.0` and `latest` dist-tag.
- Installed CLI smoke from the published package.
- Issue #1696 closed as completed.
- Phase #1680 closed as completed.
- Milestone #15 has no open issues.
