# v0.17.1 Release Readiness And Closeout Evidence

## Scope

- Work Item: `WI-1658`
- Issue: `#1658`
- Parent FR: `#1480`
- Phase: `#1476`
- Target release: `v0.17.1`
- npm package: `@mc-and-his-agents/loom@0.17.1`
- Branch: `work/1658-release`

This release publishes the post-v0.17.0 context-safe runtime line after #1488 closed the documentation and migration boundary. It consumes the already merged output envelope, configurable budget, artifact locator, explicit full-output mode, global CLI default, Codex user-level plugin payload, thread handoff, and closeout-resolver hardening work. It does not add a new runtime behavior change in this release-prep branch.

## Support Boundary

Release notes for `v0.17.1` must say that the supported post-v0.17.0 operating surface is:

- metadata-only host repositories with adoption metadata and work fact carriers;
- the global `loom` CLI as the only supported runtime execution surface;
- the Codex user-level Loom plugin as the supported agent entry payload.

Release notes must not recommend repo-local plugin/runtime/skills installation paths, single-skill package distribution, old installer compatibility paths, or downstream repository runtime installation.

## Milestone 11 Readback

- #1481, #1482, #1483, #1484, #1485, #1486, #1487, #1488, and #1493 are release inputs for #1658.
- #1488 closed after PR #1669 and carrier sync PR #1670; #1658 consumes #1488 documentation/migration wording as release-note input.
- #1489 waits for #1658 release evidence before final regression and closeout.

## Candidate Occupancy

- 2026-06-21T03:05Z pre-bump readback classified `v0.17.1` as `unpublished`:
  - git tag `v0.17.1`: missing.
  - GitHub Release `v0.17.1`: missing.
  - npm `@mc-and-his-agents/loom@0.17.1`: missing; latest was `0.17.0`.
  - matching `loom-cli-release` workflow run for the current target commit: missing.
- Current version authority in this PR:
  - `VERSION`: `v0.17.1`
  - `package.json`: `0.17.1`
  - Root package: `@mc-and-his-agents/loom`
  - Bin: `loom`

## Consumed Behavior

- Default agent-facing command output uses `agent_safe_summary_or_json_within_budget` where supported.
- Default stdout hard budget is configurable with `LOOM_AGENT_SAFE_STDOUT_BUDGET_BYTES`.
- Summary target is configurable with `LOOM_AGENT_SAFE_SUMMARY_TARGET_BYTES`.
- Complete diagnostics are emitted to artifact locators when over budget, with `LOOM_OUTPUT_ARTIFACT_DIR` controlling the artifact directory.
- Full stdout JSON is only available through explicit `--full-output` on supported command families.
- Codex skill payloads consume the global `loom` CLI output boundary and do not reimplement a vendored runtime inside target repositories.

## Pre-Merge Validation Commands

Pre-merge validation passed on branch `work/1658-release` before PR creation on 2026-06-21T03:13Z. It must be rerun at the release PR head before review and PR gate.

- `python3 tools/loom.py release readback --target . --version v0.17.1 --package @mc-and-his-agents/loom --repo MC-and-his-Agents/Loom --release-judgment release_required --json`
- `python3 tools/version_surface_check.py`
- `python3 tools/check_release_surface.py`
- `python3 tools/check_npm_package.py`
- `npm run test:package`
- `npm pack --dry-run --json --ignore-scripts`
- `python3 test/output_envelope_test.py`
- `python3 tools/loom.py help --json`
- `python3 tools/loom.py suite validate --target . --item WI-1658 --json`
- `python3 tools/loom.py suite evidence validate --target . --item WI-1658 --json`
- `python3 tools/loom.py suite carrier validate --target . --item WI-1658 --json`
- `python3 tools/loom.py fact-chain --target . --json`
- `git diff --check`

## Publish Boundary

Merging the release PR to `main` is publish-capable because this branch changes `VERSION` and root `package.json`. The expected publish mechanism is the existing `loom-cli-release` workflow triggered by push to `main` after controlled merge.

The publish action is authorized by the active milestone/11 closeout goal. The workflow must still fail closed if the version is already occupied, package metadata does not match `VERSION`, `NPM_TOKEN` is unavailable, or release evidence becomes partial.

## Post-Merge Release Closeout Evidence

Pending until the release PR is merged and `loom-cli-release` completes on `main`.

Required readback:

- release PR number, PR head, merge commit, target branch, and target branch readback;
- `loom-cli-release` run id/URL, event `push`, ref `main`, conclusion, and head SHA bound to the merge commit;
- git tag `v0.17.1` resolves to the merge commit;
- GitHub Release `v0.17.1` URL and published state;
- npm registry readback for `@mc-and-his-agents/loom@0.17.1`, including dist-tag state;
- installed/global CLI smoke using the published package;
- installer non-advancement evidence: deprecated `loom-installer` remains historical and does not become release evidence.

## Single-Person Review Boundary

This repository does not require a non-author GitHub reviewer approval for #1658. The merge gate must consume the authored Loom review record for the current PR/head. Author or agent oral judgment, GitHub comments, CI-only success, and raw runtime evidence do not satisfy approval without that review record.
