# v0.22.1 Release Readiness And Closeout Evidence

## Scope

- Bug issue / release tracker: `#1822`
- Implementation PR: `#1823`
- Target release: `v0.22.1`
- npm package: `@mc-and-his-agents/loom@0.22.1`
- Release branch: `work/1822-v0.22.1-release`

This patch release publishes the `loom resume` recovery fix for terminal
carriers that use `Current Checkpoint: closeout`. The implementation normalizes
`closeout` to `closed_out` in the shared Loom flow runtime copies and adds
focused governance-closeout contract coverage.

This release does not introduce a new plugin surface version, host adapter
version, skills registry version, deprecated installer release, new PR intent
profile, or any bypass for review, PR gate, merge-ready, release readback, host
reconciliation, or closeout evidence.

## Implementation Readback

- PR `#1823` merged to `main` at `82d9e1867fdb5454b567e0217fcae6d9978db5e6`
  on 2026-06-30T13:31:45Z.
- Hosted checks for PR `#1823` passed on head
  `31abfaa65d221655e1dc0a11ebadb9caba6b15e4`: `py-compile`,
  `demo-bootstrap`, `repo-local-cli`, `root-self-governance`, `loom-check`,
  `loom-pr-merge-gate`, `node-installer-pr`, and `release-judgment`.
- Issue `#1822` remains open until this release readback passes.

## Candidate Occupancy

- 2026-06-30T13:33:34Z pre-release readback classified `v0.22.1` as
  unoccupied / missing:
  - `git ls-remote --tags origin refs/tags/v0.22.1` returned no tag.
  - `gh release view v0.22.1 --json ...` returned `release not found`.
  - `npm view @mc-and-his-agents/loom@0.22.1 version --json` returned `E404`.
- Current version authority in this PR:
  - `VERSION`: `v0.22.1`
  - `package.json`: `0.22.1`
  - Root package: `@mc-and-his-agents/loom`
  - Bin: `loom`

## Plugin Payload Metadata

- Plugin surface version remains `0.4.0`.
- Candidate plugin payload metadata:
  - `source_package`: `@mc-and-his-agents/loom`
  - `source_package_version`: `0.22.1`
  - `source_git_sha`: `unreleased`
  - `source_git_sha_status`: `pending_release_commit`
  - `plugin_payload_version`: `0.22.1`
  - `plugin_payload_hash`: `9cc9fac5e10d10409e3ba6227b53daf8abffb2d462eb45915b341dcb11901d75`

## Pre-Merge Validation Commands

The release PR must run these before review and PR gate:

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/py_compile_clean.py tools/loom.py tools/check_cli_contract.py tools/check_npm_package.py tools/skills_surface.py tools/stamp_plugin_payload_metadata.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_cli_contract.py --surface governance-closeout`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py --surface plugin-payload-hash`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py --surface runtime-copy-parity`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py skills release-check --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/version_surface_check.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_release_surface.py`
- `npm run test:package`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py pr-intent check --intent release-only --target . --item WI-1822 --issue 1822 --branch work/1822-v0.22.1-release --head-sha <release-pr-head> --base origin/main --body-file .loom/runtime/pr/WI-1822-release-only-body.md --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py release readback --target . --version v0.22.1 --package @mc-and-his-agents/loom --repo MC-and-his-Agents/Loom --commit <release-pr-head> --release-judgment release_required --json --full-output`
- `git diff --check`

Expected pre-merge result: local checks pass, PR intent release-only check
passes for the current head/body, and release readback classifies `v0.22.1` as
not yet published with only tag, GitHub Release, npm, and workflow-run gaps.

## Pre-Merge Validation Readback

2026-06-30 local validation for release candidate head
`d6e612211b9bda55e2fb692dc1437ab280611b6b` passed:

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/py_compile_clean.py tools/loom.py tools/check_cli_contract.py tools/check_npm_package.py tools/skills_surface.py tools/stamp_plugin_payload_metadata.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_cli_contract.py --surface governance-closeout`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py skills release-check --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/version_surface_check.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_release_surface.py`
- `npm run test:package`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py pr-intent check --intent release-only --target . --item WI-1822 --issue 1822 --branch work/1822-v0.22.1-release --head-sha d6e612211b9bda55e2fb692dc1437ab280611b6b --base origin/main --body-file .loom/runtime/pr/WI-1822-release-only-body.md --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py pr metadata-preflight --target . --surface merge_ready --body-file .loom/runtime/pr/WI-1822-release-only-body.md --json`
- `git diff --check`

`PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py release readback --target .
--version v0.22.1 --package @mc-and-his-agents/loom --repo
MC-and-his-Agents/Loom --commit
d6e612211b9bda55e2fb692dc1437ab280611b6b --release-judgment
release_required --json --full-output` passed with verdict `missing`; gaps were
limited to `tag_missing`, `github_release_missing`, `npm_version_missing`, and
`workflow_run_target_commit_missing`, which is the expected pre-merge state.

## Publish Boundary

Merging the release PR to `main` is publish-capable because this branch changes
`VERSION`, root `package.json`, plugin payload release metadata/hash, and release
readiness evidence. The expected publish mechanism is the existing
`loom-cli-release` workflow triggered by push to `main`.

The workflow must still fail closed if the version is already occupied, package
metadata does not match `VERSION`, `NPM_TOKEN` is unavailable, runtime parity
fails, or release evidence becomes partial.

## Post-Merge Release Closeout Contract

After the release PR merges, closeout must read back:

- Release PR number, PR head, merge commit, and merge time.
- `loom-cli-release` workflow run for the merge commit.
- Git tag `v0.22.1` and GitHub Release `v0.22.1`.
- npm `@mc-and-his-agents/loom@0.22.1` and `latest` dist-tag.
- Published package plugin payload metadata: `source_package_version=0.22.1`,
  `plugin_payload_version=0.22.1`, `source_git_sha=<merge commit>`, and a
  matching `plugin_payload_hash`.
- Installed/global CLI smoke from the published package.
- Issue `#1822` closed after release readback passes.
