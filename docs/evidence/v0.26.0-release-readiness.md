# v0.26.0 Release Readiness

Generated: 2026-07-01

## Scope

`v0.26.0` ships the single-repo runtime-upgrade safe lane work from #1859:

- #1860 defines the runtime-upgrade safe lane and carrier-only closeout boundary.
- #1861 implements runtime-upgrade PR creation/update with metadata readback.
- #1862 implements runtime-upgrade closeout host readback and carrier PR orchestration.
- #1863 implements carrier-only review/head fallback and hosted gate consistency.
- #1864 updates runtime-upgrade lane help, README, Chinese README, and regression coverage.
- #1865 covers version, package metadata, release validation, GitHub Release, npm readback, and closeout.

The implementation PR #1866 merged to main at `06f85307587308121627bbf2f6603dc96f629721`.

## Boundaries

- This release does not add multi-repo batching.
- This release does not default to automatic merge or automatic product issue closure.
- This release does not lower current-head review, PR metadata, hosted gate, release readback, or closeout evidence requirements.
- This release does not turn `suite not_applicable` into a test bypass.
- This release does not bump Codex plugin surface version, host adapter version, skills registry version, or skill contract versions.
- The PR-event release checks are read-only; publication happens only after the release PR merges to `main`.

## Validation

Pre-release validation completed before opening the release PR:

- `python3 tools/py_compile_clean.py tools/loom.py tools/check_cli_contract.py tools/check_npm_package.py tools/stamp_plugin_payload_metadata.py tools/version_surface_check.py`
  - Result: pass (`py_compile_clean: OK (5 files)`).
- `python3 tools/version_surface_check.py`
  - Result: pass.
- `python3 tools/check_release_surface.py`
  - Result: pass.
- `python3 tools/check_npm_package.py --surface aggregate`
  - Result: pass.
  - Package: `@mc-and-his-agents/loom@0.26.0`.
  - npm payload file count: `388`.
  - plugin payload hash: `c451654a83380621ed7af0ff2eafa461512a0cc8c67d6373f4429ffc5526c00e`.
  - plugin payload file count: `173`.
- `npm pack --dry-run --json --ignore-scripts`
  - Result: pass.
  - Tarball: `mc-and-his-agents-loom-0.26.0.tgz`.
  - Entry count: `388`.
  - shasum: `a3a59615ca1cfcc35e120d7763a9a42adfe8af79`.
  - integrity: `sha512-w+xbn9mnQyrtivrjziI9+QtWUs91NQ45mzIdOR3D5K84HHVcsFD/faU2Xi8LjtrmlAw0olsnEJwwmIMw2ZPw2A==`.
- `python3 tools/check_cli_contract.py --surface release-readback`
  - Result: pass in 1.93s.
- `python3 tools/check_cli_contract.py --surface aggregate`
  - Result: pass in 350.93s.
- `python3 tools/loom.py suite validate --target . --item WI-1865 --json`
  - Result: pass.
- `python3 tools/loom.py suite evidence validate --target . --item WI-1865 --json`
  - Result: pass.
- `python3 tools/loom.py suite carrier validate --target . --item WI-1865 --json`
  - Result: pass.
- `python3 .loom/bin/loom_init.py fact-chain --target .`
  - Result: pass.
- `python3 tools/loom.py release readback --target . --version v0.26.0 --release-judgment release_required --json`
  - Result: pass, verdict `missing` as expected before the release PR is merged and published.
  - Current gaps are expected pre-publication gaps: `tag_missing`, `github_release_missing`, `npm_version_missing`, and `workflow_run_not_success`.

## Release Readback

Release is complete only after `v0.26.0` tag, GitHub Release, npm package readback, workflow readback, plugin payload metadata, and Loom carrier closeout all agree on the same release commit.
