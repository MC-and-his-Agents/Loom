# v0.19.0 Release Readiness And Closeout Evidence

## Scope

- Work Item: `WI-1718`
- Issue: `#1718`
- Parent FR: `#1711`
- Milestone: `#16`
- Target release: `v0.19.0`
- npm package: `@mc-and-his-agents/loom@0.19.0`
- Branch: `work/1718-v0.19.0-release-closeout`

This release publishes the plugin payload freshness capability delivered by the #1711 issue tree. It consumes plugin payload version/hash authority, release-bound plugin metadata, host plugin source/cache/runtime readback, CLI freshness diagnostics, refresh guidance, regression fixtures, and the legacy installer tombstone package boundary.

This release does not introduce a new plugin surface version, host adapter version, skills registry version, skill contract version, deprecated installer release, or single-skill install surface.

## Issue Tree Readback

- #1712, #1713, #1714, #1715, #1716, #1717, #1719, #1720, #1721, #1722, and #1732 are closed.
- #1718 is the active release closeout issue.
- #1711 remains open until #1718 release readback passes and no stale child issue remains.

## Candidate Occupancy

- 2026-06-23 pre-bump readback classified `v0.19.0` as unoccupied:
  - npm `@mc-and-his-agents/loom@0.19.0`: missing; latest was `0.18.0`.
  - GitHub Release `v0.19.0`: missing.
  - git tag `v0.19.0`: missing.
- Current version authority in this PR:
  - `VERSION`: `v0.19.0`
  - `package.json`: `0.19.0`
  - Root package: `@mc-and-his-agents/loom`
  - Bin: `loom`

## Plugin Payload Metadata

- Plugin surface version remains `0.4.0`.
- Candidate plugin payload metadata:
  - `source_package`: `@mc-and-his-agents/loom`
  - `source_package_version`: `0.19.0`
  - `source_git_sha`: `unreleased`
  - `source_git_sha_status`: `pending_release_commit`
  - `plugin_payload_version`: `0.19.0`
  - `plugin_payload_hash`: `24ab237b9cbe00e5bc716191e5bc11b47913f8080d012220564237f8e2639ba9`
- Publish workflow stamps `source_git_sha` to the final main push commit and recomputes `plugin_payload_hash` before `npm publish`.

## Consumed Behavior

- `loom version --json`, `loom doctor`, `loom upgrade-plan`, and `loom host doctor` report CLI and plugin payload freshness.
- Plugin freshness compares source, registered Codex plugin source, and runtime cache payload metadata/hash.
- Stale payload guidance routes users to `loom host install/register --host codex --scope user`.
- `loom install/upgrade --target <repo>` remains target-repository installed-state scope, not the host plugin refresh path.
- Legacy `@mc-and-his-agents/loom-installer` is retired as an active installer and retained only as a tombstone/migration artifact.

## Pre-Merge Validation Commands

The release PR ran these locally before review and PR gate on 2026-06-22T22:49Z:

- `CODEX_EXPORT_GH_TOKEN=1 PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py release readback --target . --version v0.19.0 --package @mc-and-his-agents/loom --repo MC-and-his-Agents/Loom --release-judgment release_required --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/stamp_plugin_payload_metadata.py --source-git-sha unreleased --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/version_surface_check.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_release_surface.py`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/check_npm_package.py`
- `npm run test:package`
- `npm pack --dry-run --json --ignore-scripts`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite validate --target . --item WI-1718 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite evidence validate --target . --item WI-1718 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py suite carrier validate --target . --item WI-1718 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py fact-chain --target . --item WI-1718 --json`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_flow.py state-check --target . --item WI-1718`
- `git diff --check`

Result: pass. The release readback command classified `v0.19.0` as unpublished/unoccupied, which is the expected pre-merge state.

## Publish Boundary

Merging the release PR to `main` is publish-capable because this branch changes `VERSION`, root `package.json`, plugin payload release metadata, and release workflow publication behavior. The expected publish mechanism is the existing `loom-cli-release` workflow triggered by push to `main`.

The workflow must still fail closed if the version is already occupied, package metadata does not match `VERSION`, `NPM_TOKEN` is unavailable, or release evidence becomes partial.

## Legacy Installer Deprecation Boundary

`@mc-and-his-agents/loom-installer` remains a retired tombstone artifact. This release does not bump or publish that package.

Running `npm deprecate @mc-and-his-agents/loom-installer` is an external registry write and requires separate explicit confirmation before execution. It is not bundled into automatic v0.19.0 publication.

## Post-Merge Release Closeout Contract

After the release PR merges, closeout must read back:

- Release PR number, PR head, merge commit, and merge time.
- `loom-cli-release` workflow run for the merge commit.
- Git tag `v0.19.0` and GitHub Release `v0.19.0`.
- npm `@mc-and-his-agents/loom@0.19.0` and `latest` dist-tag.
- Published package plugin payload metadata: `source_package_version=0.19.0`, `plugin_payload_version=0.19.0`, `source_git_sha=<merge commit>`, and a matching `plugin_payload_hash`.
- Installed/global CLI smoke from the published package.
- Codex host plugin source/cache readback after `loom host install/register --host codex --scope user --apply`.
- Issue #1718 closed as completed.
- Parent FR #1711 closed as completed.
