# WI-1403 Progress

## Dynamic Facts

- Item ID: WI-1403
- Current Checkpoint: build
- Current Stop: T1403 has implemented and locally validated demo bootstrap canonicalization diagnostics; PR creation/update and PR/head/body/check readback remain before scheduler gate handoff.
- Next Step: Commit and push branch `work/1403-demo-canonicalization-diagnostics`, open/update PR for issue #1403, read back PR metadata/head/checks, then report `waiting-scheduler-gate`.
- Blockers: None
- Latest Validation Summary: Local validation passed for the implemented diagnostic surface: `git diff --check`; `python3 tools/check_demo_bootstrap_fixture.py --help` read back `--surface {aggregate,generation,canonicalization,fixture-drift}`; `python3 tools/check_demo_bootstrap_fixture.py --surface canonicalization --timeout 180`; `make loom-demo-new-project-canonicalization-check`; `python3 tools/check_demo_bootstrap_fixture.py --surface generation --timeout 180`; `python3 tools/check_demo_bootstrap_fixture.py --surface fixture-drift --show-surface-evidence --timeout 180`; `python3 tools/check_demo_bootstrap_fixture.py --surface aggregate --show-surface-evidence --timeout 180` with `subsurface_count=3`; `python3 tools/check_demo_bootstrap_fixture.py --surface aggregate --timeout 180`; `make loom-demo-new-project-check`; targeted negative diagnostics for invalid JSON and missing host-dynamic section labels returned `demo-bootstrap-canonicalization-invalid-json` and `demo-bootstrap-canonicalization-incomplete` with `evidence_locator="tools/check_demo_bootstrap_fixture.py --surface canonicalization"`; `python3 tools/py_compile_clean.py tools/check_demo_bootstrap_fixture.py`; `python3 tools/loom.py suite inspect --target . --item WI-1403 --json`; `python3 tools/loom.py suite validate --target . --item WI-1403 --json` returned `result=not_applicable`, `blocking_gaps=[]`, exit 1 per current not_applicable contract; `git status --short --branch -- examples/new-project` showed no tracked fixture changes.
- Recovery Boundary: WI-1403 owns only demo bootstrap canonicalization diagnostics in `tools/check_demo_bootstrap_fixture.py`, a narrow Makefile target alias, WI-1403 suite/progress carriers, and PR metadata. It must not change #1401 generation semantics, #1402 fixture drift/cleanliness split, #1404 docs/evidence convergence, parent #1262 closeout, fixture contents, generated runtime behavior, release/package behavior, review artifacts, guardian/formal review, controlled merge, or closeout.
- Current Lane: demo-bootstrap-canonicalization-diagnostics

## Execution Ledger

- Ledger Binding: recovery_entry
- Plan Locator: GitHub issue #1403
- Acceptance Locator: GitHub issue #1403 acceptance criteria
- Validation Evidence Locator: `.loom/progress/WI-1403.md`; local command output for `tools/check_demo_bootstrap_fixture.py --surface canonicalization`, existing generation and fixture-drift surfaces, aggregate fixture validation, suite inspect/validate, py compile, PR metadata/head readback, and hosted checks classification.
- Handoff Notes Locator: scheduler thread 019eb28d-ac3b-7623-8955-12542fa2e08d / worker T1403 reports
- Evidence Freshness: current before PR metadata/head readback
