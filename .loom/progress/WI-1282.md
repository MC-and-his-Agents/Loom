# WI-1282 Progress

## Dynamic Facts

- Item ID: WI-1282
- Current Checkpoint: closed_out
- Current Stop: PR #1385 merged at 2026-06-09T13:57:11Z as merge commit 0dbcaab1b03c3c1bc9725d37604110e170eafe18; WI-1282 implementation and scheduler-owned gate are complete.
- Next Step: Merge this closeout-only carrier sync, then run reconciliation sync to close issue #1282 and unblock #1283/#1284.
- Blockers: None
- Latest Validation Summary: 2026-06-09 review-shadow correction at PR #1385 head 6fec676e80c8ed631ed4a71a1d9af608679dfbb5: official recovery writeback moved WI-1282 to merge checkpoint; current-head allow review was recorded in .loom/reviews/WI-1282.json after reviewing the frozen repo-local-cli workflow split and WI-1282 carriers; carrier refresh dry-run and write were limited to authorized .loom/shadow/closeout-loom.json and .loom/shadow/merge-ready-loom.json; git diff --check passed; workflow readback confirmed the stable repo-local-cli group order and the 13 preserved commands including setup-demo-bootstrap and runtime-state scene conflict negative check; fact-chain and loom_init verify passed with current item WI-1282; shadow-parity --surface all --blocking passed; adopt verify passed. Pre-commit flow review is expected to purity-block on uncommitted authorized shadow carriers; clean-worktree purity/flow review/pr-gate/PR metadata preflight remain pending after commit.
- Recovery Boundary: Closeout-only sync for WI-1282. Consume completed facts from PR #1385 merge commit 0dbcaab1b03c3c1bc9725d37604110e170eafe18; do not change repo-local-cli workflow semantics, #1283/#1284/#1259 work, Round 5, Round 7+, Deferred roadmap, release/package behavior, generated runtime, guardian, or controlled merge.
- Current Lane: repo-local-cli-surfaces

## Post-Merge Closeout Evidence

- PR: #1385 merged at 2026-06-09T13:57:11Z.
- Head SHA: c4d5521d05f691e82780e3ade279b8a179684088.
- Merge Commit: 0dbcaab1b03c3c1bc9725d37604110e170eafe18 on `origin/main`.
- Hosted Checks: `py-compile`, `demo-bootstrap`, `repo-local-cli`, `root-self-governance`, `loom-check`, `node-installer-pr-gate/gate`, and `loom-pr-merge-gate` passed for the merged head. The final `loom-pr-merge-gate` pass was rerun attempt 2 job 80339884500 after a stale PR body race in attempt 1.
- Scheduler Gates: `pr-metadata preflight`, `pr-gate check`, `flow merge-ready`, `controlled-merge check`, and controlled merge execution passed before host merge delegation.
- Closeout Gate: `closeout check` consumes retained review, merge backlink, and required-check evidence; remaining host drift is issue #1282 still open and must be reconciled after this closeout carrier lands.

## Command Group Contract

Stable repo-local-cli command group order for downstream #1283/#1284 consumption:

| Order | Group name | Workflow step name | Commands / execution surface |
| --- | --- | --- | --- |
| 0 | `setup-demo-bootstrap` | `repo-local-cli: setup-demo-bootstrap` | `make loom-demo-new-project-check` from repo root. This remains before every command that consumes `examples/new-project`. |
| 1 | `init-runtime` | `repo-local-cli: init-runtime` | `python3 .loom/bin/loom_init.py runtime-state --target .`; `python3 .loom/bin/loom_init.py verify --target .` with `working-directory: examples/new-project`. |
| 2 | `fact-chain` | `repo-local-cli: fact-chain` | `python3 .loom/bin/loom_init.py fact-chain --target .`; `python3 .loom/bin/loom_flow.py runtime-state --target . --item INIT-0001`; `python3 .loom/bin/loom_flow.py fact-chain --target . --item INIT-0001`; `python3 .loom/bin/loom_flow.py runtime-evidence --target . --item INIT-0001`; `python3 .loom/bin/loom_flow.py state-check --target . --item INIT-0001` with `working-directory: examples/new-project`. |
| 3 | `flow-gates` | `repo-local-cli: flow-gates` | `python3 .loom/bin/loom_flow.py flow pre-review --target . --item INIT-0001`; `python3 .loom/bin/loom_flow.py checkpoint admission --target . --item INIT-0001` with `working-directory: examples/new-project`. |
| 4 | `workspace-locate` | `repo-local-cli: workspace-locate` | `python3 .loom/bin/loom_flow.py workspace locate --target . --item INIT-0001` with `working-directory: examples/new-project`. |
| 5 | `purity-check` | `repo-local-cli: purity-check` | `python3 .loom/bin/loom_flow.py purity-check --target . --item INIT-0001` with `working-directory: examples/new-project`. |
| 6 | `runtime-state-scene-conflict-negative` | `repo-local-cli: runtime-state-scene-conflict-negative` | `LOOM_SOURCE_REPO_ROOT="$PWD" LOOM_INSTALLED_SKILLS_ROOT="$PWD/skills" LOOM_RUNTIME_SCENE=upgrade-rehearsal python3 skills/shared/scripts/loom_flow.py runtime-state --target examples/new-project --item INIT-0001` from repo root; the command must fail closed or the step fails. |

Grouping rationale:

- `cd examples/new-project` from the original shell block is preserved as step-level `working-directory: examples/new-project` for groups 1-5. This keeps each CI step independently diagnosable without duplicating `cd` in every shell block.
- The `fact-chain` group intentionally includes runtime-state, fact-chain, runtime-evidence, and state-check flow reads because their original order is a single carrier/fact-chain validation sequence before flow gates.
- The runtime-state scene conflict negative check remains a separate root-level group so `LOOM_SOURCE_REPO_ROOT="$PWD"` continues to bind the source repo root, not `examples/new-project`.
- No repo-local CLI command from the #1281 inventory is removed or weakened; command order remains setup -> init/runtime -> fact-chain/state reads -> flow gates -> workspace locate -> purity check -> negative runtime-state scene conflict.

## Status Carrier Boundary

- Scheduler correction `T1-correction-202606091835-fact-chain` authorizes `.loom/bootstrap/init-result.json`, `.loom/work-items/WI-1282.md`, and derived `.loom/status/current.md` updates so the current fact-chain item can become WI-1282 for PR #1385 gate input.
- `.loom/status/current.md` must be regenerated through the official work-item activation path, not edited by hand.
- Recheck condition: rerun fact-chain, verify, WI-1282 suite/evidence/carrier checks, PR metadata preflight, and hosted check readback after the corrective head is pushed.

## Execution Ledger

- Ledger Binding: recovery_entry
- Plan Locator: not_applicable
- Acceptance Locator: GitHub issue #1282 acceptance criteria
- Validation Evidence Locator: .loom/specs/WI-1282/evidence-map.md
- Handoff Notes Locator: worker reports to scheduler thread 019eabaf-92dc-7a52-a238-838f4c0bf4ac
- Evidence Freshness: current
