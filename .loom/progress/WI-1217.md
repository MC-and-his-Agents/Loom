# WI-1217 Progress

## Dynamic Facts

- Item ID: WI-1217
- Current Checkpoint: merge
- Current Stop: PR #1227 is open for `work/1217-metadata-only-adoption` at head `322eafca74075d91ffcb7346962778b41df22a6e`; first implementation batch is committed and CI is blocked because root self-governance was still bound to stale WI-1204 carriers.
- Next Step: Commit WI-1217 carriers, refresh authored review record, rerun local gate checks, push, validate CI, then continue issue closeout and release/no-release decision after target branch validation.
- Blockers: `python3 tools/check_cli_contract.py`, `root-self-governance`, and `loom-pr-merge-gate` currently block on stale WI-1204 active carrier/review binding, not on metadata-only fixture behavior.
- Latest Validation Summary: Passing local evidence: `git diff --check`; `python3 -m json.tool plugins/loom/.codex-plugin/plugin.json`; `python3 tools/check_release_surface.py`; `python3 tools/skills_surface.py check`; targeted metadata-only fixture passed `loom install --mode metadata-only`, `installed-state validate`, `host verify --mode metadata-only`, `skills check`, and `detect`; targeted metadata-only pollution fixture blocked unexpected `plugins/loom/skills`; targeted embedded fixture passed `host install --mode plugin` and `host verify --mode plugin`. Blocking evidence: `python3 tools/check_cli_contract.py` fails on stale WI-1204 review/scope, and PR #1227 CI fails `root-self-governance` / `loom-pr-merge-gate` for the same stale active carrier binding.
- Recovery Boundary: Do not make metadata-only write or require `plugins/loom/skills`, `.agents/skills`, or root `skills`; do not encode user workstation registration as repo truth; do not delete downstream repo-owned governance evidence; preserve embedded payload mode compatibility.
- Current Lane: loom-metadata-only-adoption

## Execution Ledger

- Ledger Binding: recovery_entry
- Plan Locator: .loom/specs/WI-1217/plan.md
- Acceptance Locator: .loom/specs/WI-1217/spec.md
- Validation Evidence Locator: .loom/specs/WI-1217/evidence-map.md
- Handoff Notes Locator: not_applicable
- Evidence Freshness: current
