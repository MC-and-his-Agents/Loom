{
  "engine": "codex",
  "adapter": "loom/default-codex",
  "profile": {
    "schema_version": "loom-review-engine-profile/v1",
    "profile_id": "high-risk",
    "adapter": "loom/default-codex",
    "engine": "codex",
    "model": "gpt-5.2",
    "reasoning_effort": "high",
    "timeout_seconds": null,
    "context_policy": "expanded-risk-baseline",
    "selection_reason": "risk terms in the active item require the high-risk formal review profile",
    "override_reason": null
  },
  "context_pack": ".loom/runtime/review/WI-763/f7f91781d75301b624a4b8cc937f8bce2666b9ad/context-pack.json",
  "result": "pass",
  "reviewed_head": "f7f91781d75301b624a4b8cc937f8bce2666b9ad",
  "decision": "block",
  "summary": "WI-763 implements the PR-local semantic approval gate (`pr-gate`) and the controlled-merge wrapper, and documents the PR #762 regression. However, the host workflow as written is likely to run the gate on a non-head checkout for `pull_request` events, which causes `pr-gate` to fail closed via `checkout_head_drift`. In addition, the authored implementation review record at `.loom/reviews/WI-763.json` remains scaffolded (`decision=fallback`), so the merge checkpoint and PR gate cannot pass until a real formal review is recorded and bound to the current head + validation summary. Live GitHub readback proof that `loom-pr-merge-gate` is required on `main` is still a required deferred follow-up before claiming host enforcement closure.",
  "kind": "code_review",
  "validation_summary": "2026-05-16 local validation passed after WI-763 spec carrier, spec review, Codex CLI drift fix, runtime-evidence carrier boundary, and strict OpenAI response schema fix: codex exec 0.120.0 no longer accepts --ignore-user-config, so Loom default review engine now preserves codex exec --output-schema without that removed flag; OpenAI response schema now requires additionalProperties:false on nested object schemas, so Loom review result schema was tightened for findings/disposition/code_location; current item runtime evidence may be versioned as carrier-only drift but still cannot satisfy approval truth; PYTHONPYCACHEPREFIX=/tmp/loom-pycache python3 -m py_compile for touched Loom runtime entrypoints -> OK; make skills-check -> OK; git diff --check -> OK; python3 tools/loom_flow.py adopt verify --target . --item WI-763 -> pass; python3 tools/loom_flow.py shadow-parity --target . -> pass; MISE_NO_CONFIG=1 LOOM_INSTALLER_TEST_PYTHON_BIN=/Users/claw/.local/share/mise/installs/python/3.12.13/bin/python3.12 python3 tools/loom_check.py . -> OK (36 surfaces)."
}
