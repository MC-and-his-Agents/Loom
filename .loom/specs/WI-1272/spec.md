# WI-1272 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1272 is a narrow executable CLI contract surface split for suite carrier inspect/validate fixtures only, without changing task-carrier runtime validation semantics or suite contract/evidence semantics; the existing aggregate `check-cli-contract` run remains the compatibility consumer for the same carrier assertions. consumer boundary: suite validate, review, merge-ready, PR gate, hosted CI, and issue closeout may consume this locator only as formal suite non-applicability; implementation review, fact-chain, PR metadata/head binding, local CLI contract validation, hosted checks, controlled merge, no-release judgment, and closeout evidence remain required. recheck condition: require a full or minimal suite if this PR expands beyond the carrier-only named surface split, changes task-carrier runtime validation semantics, changes suite contract/evidence semantics, touches #1273/#1274/#1257 scope, changes hosted workflow behavior, or modifies metadata schema. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1272 carriers and `tools/check_cli_contract.py`. review requirement: scheduler-owned semantic review must review the current PR head before merge-ready.
