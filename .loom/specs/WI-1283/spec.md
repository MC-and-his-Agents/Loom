# WI-1283 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1283 adds Loom source repo local Makefile aliases and documentation for reproducing the already-frozen `repo-local-cli` CI command groups from #1282. It preserves the existing command group names, order, command membership, and runtime-state scene conflict negative check, and it does not change runtime command semantics, workflow step identity, generated runtime, parser/gate behavior, release/package behavior, permissions, or external-visible actions. consumer boundary: this decision only skips formal suite artifacts; review, fact-chain/status readback, PR metadata/head binding, command group readback, local fast/full alias validation, hosted CI readback, release/no-release evidence, and scheduler-owned gate consumption remain required. recheck condition: require a minimal or full suite if the PR expands beyond `Makefile`, repo-local validation docs, WI-1283 carriers, and #1283 PR metadata, or if it changes `.github/workflows/loom-check.yml`, runtime CLI behavior, `tools/`, `.loom/bin`, generated skills/runtime copies, release/package workflows, metadata schema, gate/parser behavior, fixture contents, permissions, external-visible behavior, or #1284/#1259 closeout/evidence ownership. scope proof: `git diff origin/main...HEAD` must remain limited to the assigned alias/docs implementation and WI-1283 carriers. review requirement: current-head review or scheduler-authorized equivalent must consume the final local alias map, fast/full distinction, fail-closed negative check behavior, and validation evidence before merge-ready.
