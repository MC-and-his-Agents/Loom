# Plan

1. Add one installed-state guard at the shared carrier refresh entry.
2. Add source and CLI contract assertions for metadata-only, repo-local, malformed installed-state, and checkpoint-aware closeout fixtures.
3. Generate the runtime surfaces and validate against Loom source fixtures and Harbor #244.
