# WI-1643 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1643 is a version-only release execution Work Item for the already completed milestone #14 pure global install model changes. The PR changes only `VERSION` and `package.json.version` from v0.16.0/0.16.0 to v0.17.0/0.17.0 and relies on existing release/package validation plus post-merge release readback. consumer boundary: suite validate, review, merge-ready, PR gate, hosted CI, controlled merge, release readback, and closeout may consume this locator only as the formal suite decision; current-head review, fact-chain/status readback, PR metadata/head binding, hosted checks, release_required judgment, controlled merge, workflow publish evidence, tag/GitHub Release/npm readback, and issue closeout remain required. recheck condition: require a minimal or full suite if this PR expands beyond `VERSION`, `package.json`, WI-1643 carriers, and PR metadata, or if it modifies runtime, CLI behavior, package payload, plugin payload, release workflow semantics, migration docs, permissions, or manually creates tag/GitHub Release/npm before merge. scope proof: `git diff origin/main...HEAD` must remain limited to `VERSION`, `package.json`, and WI-1643 Loom carriers/metadata. review requirement: `.loom/reviews/WI-1643.json` must review the current PR head before merge-ready, and any post-review drift must be limited to allowed release carrier or PR metadata refresh.
