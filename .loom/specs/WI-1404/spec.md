# WI-1404 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1404 is a docs/evidence convergence Work Item for an already implemented demo bootstrap validation split. It documents the generation, canonicalization, fixture-drift, examples/new-project cleanliness, and aggregate evidence surfaces from merged #1401, #1403, and #1402 without changing validation behavior, fixture content, generated runtime behavior, release/package behavior, permissions, or external-visible actions. consumer boundary: this decision only skips formal suite artifacts; current-head implementation review, PR metadata/head binding, evidence-map and task-carrier readback, local focused surface validation, aggregate demo bootstrap validation, tracked `examples/new-project` cleanliness readback, hosted checks, scheduler-owned gate consumption, controlled merge, no_release judgment, and parent closeout consumption remain required. recheck condition: require a minimal or full suite if the PR expands beyond concise docs/methodology command/evidence references and WI-1404 carriers, or if it changes `tools/check_demo_bootstrap_fixture.py`, fixture contents, Makefile command behavior, generated runtime behavior, release/package behavior, permissions, external-visible behavior, `.loom/reviews/**`, `.loom/status/current.md`, `.loom/shadow/**`, or parent #1262/#1255 closeout. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1404 docs/evidence references and allowed WI-1404 carriers. review requirement: scheduler-owned current-head review or gate must consume the final PR head, docs/evidence references, local validation evidence, PR metadata readback, hosted checks classification, and no_release rationale before merge-ready.
