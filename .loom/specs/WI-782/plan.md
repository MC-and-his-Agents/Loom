# WI-782 Plan

1. Inspect existing `loom_init verify` carrier and gitignore checks.
2. Add a stable carrier Git visibility check based on enabled/generated stable outputs.
3. Add regression fixtures for ignored, untracked, tracked, and runtime scratch paths.
4. Refresh generated skills surfaces from source.
5. Run targeted and repository validation.
