# WI-531 Implementation Contract

## Write Scope

- `VERSION`
- generated `skills/**/loom-package.json` repo_version metadata
- `.loom` WI-531 work item, progress, specs, review, status, and bootstrap carrier bindings

## External Actions

- Create the final closeout PR for #531.
- After merge and post-merge verification, create GitHub tag/release `v0.8.0`.
- Close #531 after release truth and issue truth agree.

## Guardrails

- Do not change completed FR implementation scope.
- Do not create the `v0.8.0` release before the closeout PR is merged to main.
- Do not close #531 before the release/tag exists at the verified final release commit.
