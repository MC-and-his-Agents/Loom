# WI-827 Plan

1. Restore and consume prerequisite issue semantics for dependency taxonomy, intake routing, repo interop boundaries, and GitHub profile upgrade preconditions.
2. Connect host binding inspection to Work Item, branch, PR, Project item, native dependency graph, provenance, freshness, findings, and fixed fallback outputs.
3. Connect Project drift into status, resume/pre-review, merge-ready, and reconciliation surfaces.
4. Connect `/goal` derive/validate, resume bootstrap, delegated execution protocol, consistency checks, and closeout completion evidence.
5. Declare advanced Governance Lint architecture/boundary surfaces and enforce the Loom core hardcoding guard.
6. Update docs, shared references, generated skills, fixtures, and tests; bump the installer package patch version because generated skill payload behavior changed.
7. Validate locally, bind PR #856 to WI-827, push, wait for CI, merge if permitted, then reconcile child and parent issues plus Project state.
