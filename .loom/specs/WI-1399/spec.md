# WI-1399 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1399 is a narrow executable validation-surface split for `tools/skills_surface.py` that separates generated skill package launcher smoke validation into a named, targetable read-only surface while preserving the aggregate `check` command and the #1397 `docs-reference-sync` / `generated-tree-drift` plus #1398 `package-metadata` / `cache-artifacts` surfaces. The issue acceptance criteria are verified through targeted launcher smoke surfaces, per-skill launcher targets, the existing #1397 and #1398 targeted surfaces, the aggregate skills check, Makefile alias readback, PR metadata/head binding, hosted checks, and scheduler-owned gate; a full or minimal formal suite would duplicate those acceptance criteria without adding a new product or governance contract. consumer boundary: suite validate, spec review, implementation review, merge-ready, PR gate, hosted CI, and issue closeout consume this locator only as the formal suite path decision; local validation, stable launcher failure-name/evidence-locator output, PR metadata/head binding, hosted checks, scheduler-owned review/gates, controlled merge, and closeout evidence remain required. recheck condition: require a full or minimal suite if this PR expands beyond #1399, rewrites the #1397 docs/reference sync or generated tree drift surfaces, rewrites the #1398 package metadata or cache artifact surfaces, implements docs/evidence convergence (#1400), release/package behavior, runtime regression behavior, generated skills content, hosted workflow semantics, PR metadata schema, gate/parser behavior, permissions, external-visible behavior, or any Round 9+ / deferred-roadmap scope. scope proof: `git diff origin/main...HEAD` must remain limited to `tools/skills_surface.py`, narrowly named Makefile launcher smoke validation target(s), WI-1399 Loom readiness carriers, and PR metadata. review requirement: current_head_review_required by scheduler; worker must not write `.loom/reviews/**`.
