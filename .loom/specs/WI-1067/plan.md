# WI-1067 Plan

1. Read #1067, #1064-#1066 closeout evidence, and current install/release/adoption docs.
2. Replace README and README.zh-CN install sections with the root CLI install path and CLI-managed host/SKILLS verification commands.
3. Replace Codex install guidance with CLI-managed install and verification.
4. Update release/adoption docs that still describe GitHub-only, full-repo, standalone plugin, standalone SKILLS, or installer-driven installs as the default path.
5. Keep installer README references historical and explicit that the package is deprecated.
6. Run local validation, open PR, consume PR and merge commit checks, and close #1067 with evidence for #1068.
