# WI-1276 Implementation Contract

- Suite path: minimal

## Contract Surface

- `tools/loom_check.py` and shared runtime copies expose `--source-surface review-run`.
- `review-run` executes the review-run fixture set under the stable `review-run-fixture` failure category.
- `source-self-fixture` remains an aggregate and continues to include review-run coverage plus the existing source-self fixture checks.
- Generated skills runtime `loom_check.py` copies stay byte-for-byte aligned with the canonical shared runtime generated by `python3 tools/loom.py skills generate --target . --apply --json`.

## Consumer Boundary

- Review and merge gates consume this contract through the focused `review-run` source surface, the aggregate `source-self-fixture` surface, generated skills parity, PR metadata, and WI-1276 carriers.
- #1277, #1278, #1279, #1280, and #1258 do not consume this contract until #1276 is merged and read back.

## Non-Goals

- Do not implement merge-gate, closeout-reconciliation, retire-workspace, installed-runtime, or parent closeout fixture surfaces in WI-1276.
- Do not change release, package, workflow, or user-visible CLI behavior beyond exposing the named diagnostic source surface.

## Validation Binding

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface review-run .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface source-self-fixture .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py skills check --target . --json`
- `make loom-demo-new-project-check`
