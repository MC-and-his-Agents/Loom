# WI-1244 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1244 is a bounded regression fixture and CLI contract validation batch for HotCP-style repositories that intentionally use the global Loom CLI runtime without `.loom/bin`, while preserving retained `.loom/bin` compatibility behavior. The GitHub issue acceptance criteria, synthetic fixture catalog, aggregate CLI contract assertions, validation evidence, PR metadata, scheduler-owned current-head review, and high-cost PR readiness gate are the governing evidence surfaces; a formal spec suite would duplicate the issue-scoped fixture contract without adding a new governance or runtime contract. consumer boundary: this decision only skips formal suite artifacts; fact-chain/status carriers, current-head implementation review, PR metadata/head binding, local aggregate fixture validation, hosted checks, PR gate, release/no-release judgment, controlled merge, and closeout evidence remain required. recheck condition: require a minimal or full suite if this PR expands beyond `tools/check_cli_contract.py`, `docs/evidence/fixtures/legacy-migration-validation-fixtures.json`, `docs/evidence/validations/validation-global-cli-runtime-fixtures.md`, and WI-1244 Loom carriers, or if it changes shared schema, parser/failure vocabulary, runtime contract semantics, release behavior, parent #1238 closeout, external-visible behavior, or any #1245/#1246 scope. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1244 regression fixture implementation, validation evidence, allowed WI-1244 carriers, and PR metadata. review requirement: current-head review must consume the final fixture matrix, no-`.loom/bin` global CLI runtime behavior, retained `.loom/bin` compatibility behavior, provider mismatch diagnostics, local validation evidence, and PR metadata before merge-ready.
