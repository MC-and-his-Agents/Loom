# WI-998 Plan

1. Bind the formal worktree, branch, and fact chain to `WI-998`.
2. Update `README.md` so the first public entrypoint is CLI-first.
3. Update `README.zh-CN.md` with the same entrypoint semantics.
4. Keep installer text scoped to compatibility shim, adapter-managed installs, single-skill helper flows, and legacy bridging.
5. Validate README wording with CLI contract checks, version surface checks, installer doc sync, adoption verification, shadow parity, PR gate, and full repository checks.
6. Merge the PR only after review evidence, host checks, and PR/head bindings are consistent.
