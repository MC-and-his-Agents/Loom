# WI-1282 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1282 is a workflow-only CI command-surface split for the existing `repo-local-cli` job. It freezes stable group names and step identity while preserving the current repo-local CLI commands, setup dependency, order-sensitive sequence, and runtime-state scene conflict negative check. It does not introduce product behavior, runtime command semantics, parser changes, generated runtime changes, release/package behavior, permissions, or external-visible actions. consumer boundary: this decision only skips formal suite artifacts; review, fact-chain/status readback, PR metadata/head binding, command preservation checks, local repo-local CLI validation, hosted CI readback, release/no-release evidence, and scheduler-owned gate consumption remain required. recheck condition: require a minimal or full suite if the PR expands beyond `.github/workflows/loom-check.yml` repo-local-cli step identity/split, WI-1282 progress/status/evidence carriers, and #1282 PR metadata, or if it changes runtime CLI behavior, `tools/`, `.loom/bin`, generated skills, release/package workflows, metadata schema, gate/parser behavior, fixture contents, permissions, or external-visible behavior. scope proof: `git diff origin/main...HEAD` must remain limited to the assigned workflow split, WI-1282 carriers, and PR metadata. review requirement: current-head review or scheduler-authorized equivalent must consume the final workflow command group map and command preservation evidence before merge-ready.
