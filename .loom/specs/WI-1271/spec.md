# WI-1271 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1271 is a narrow executable CLI contract surface split that reuses existing suite evidence and suite carrier assertions without changing runtime suite semantics; the implementation is validated through the new named surface and the preserved aggregate `check-cli-contract` run. consumer boundary: suite validate, review, merge-ready, PR gate, hosted CI, and issue closeout may consume this locator only as formal suite non-applicability; implementation review, fact-chain, PR metadata/head binding, local CLI contract validation, hosted checks, controlled merge, and closeout evidence remain required. recheck condition: require a full or minimal suite if this PR expands beyond the named surface split, changes suite evidence/carrier validator semantics, touches #1272/#1273/#1274/#1257 scope, changes hosted workflow behavior, or modifies metadata schema. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1271 carriers and `tools/check_cli_contract.py`. review requirement: scheduler-owned semantic review must review the current PR head before merge-ready.
