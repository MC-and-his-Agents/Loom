# WI-1955 Implementation Contract

## Scope

- Release authority: `VERSION`, `package.json`, `plugins/loom/.codex-plugin/plugin.json`.
- Release evidence: `docs/evidence/v0.27.1-release-readiness.md`.
- Loom carriers: `.loom/work-items/WI-1955.md`, `.loom/progress/WI-1955.md`, `.loom/status/current.md`, `.loom/specs/WI-1955/*`, `.loom/reviews/WI-1955*.json`, and closeout/merge-ready shadow evidence.

## Required Behavior

- Root `VERSION` must be `v0.27.1`.
- Root `package.json` version must be `0.27.1`.
- Plugin payload metadata must carry `source_package_version` and `plugin_payload_version` of `0.27.1`.
- Plugin payload hash must be generated by `tools/stamp_plugin_payload_metadata.py`.
- Release PR metadata must declare `change_class=release`, `suite_path=minimal`, and `release_judgment=release_required`.
- Release PR checks must remain read-only for publication.
- Post-merge closeout must wait for release readback to prove tag, GitHub Release, npm package, workflow, and package surface consistency.

## Non-Goals

- No new runtime behavior after #1928/#1930 implementation PR #1967.
- No #1933 temporary hardcoding.
- No #1935/v0.28.0 host adoption tax scope.
- No downstream repo-local `tools/loom.py` shim requirement.
- No release workflow or credential behavior change.
- No plugin surface version bump.
- No host adapter or skills contract version bump.
- No legacy installer release.
- No manual overwrite of tag, GitHub Release, or npm package.
