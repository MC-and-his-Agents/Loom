# WI-1278 Implementation Contract

- Suite path: minimal

## Contract Surface

- `tools/loom_check.py` and shared runtime copies expose `--source-surface closeout-reconciliation`.
- `closeout-reconciliation` executes closeout/reconciliation fixture checks under a stable named source surface and step name.
- `source-self-fixture` remains an aggregate and continues to include `review-run`, `merge-gate`, `closeout-reconciliation`, and the remaining source-self fixture checks.
- Existing `review-run` and `merge-gate` names are not renamed.
- Generated skills runtime `loom_check.py` copies stay aligned with the canonical shared runtime generated by `PYTHONDONTWRITEBYTECODE=1 python3 tools/skills_surface.py generate`.

## Consumer Boundary

- Review and merge gates consume this contract through the focused `closeout-reconciliation` source surface, the aggregate `source-self-fixture` surface, generated skills parity, PR metadata, hosted checks, and WI-1278 carriers.
- #1279, #1280, and #1258 do not consume this contract until #1278 is merged and read back.

## Non-Goals

- Do not implement retire-workspace, installed-runtime, or parent closeout fixture surfaces in WI-1278.
- Do not change release, package, workflow, or user-visible closeout behavior beyond exposing the named diagnostic source surface.
- Do not run scheduler-owned review artifacts, guardian/loom_check gate consumption, controlled merge, post-merge readback, or closeout.

## Validation Binding

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface closeout-reconciliation .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface source-self-fixture .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py skills check --target . --json`
