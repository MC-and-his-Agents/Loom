# WI-1403 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1403 is a narrow executable validation-surface split for `tools/check_demo_bootstrap_fixture.py`. It makes demo bootstrap init-result host/runtime canonicalization a named, targetable diagnostic surface, adds canonicalization-specific failure labels and evidence locators, and preserves the existing generation, fixture-drift, and aggregate fixture validation contract. A formal spec suite would duplicate the GitHub issue acceptance criteria without adding a new product or governance contract. consumer boundary: this decision only skips formal suite artifacts; current-head implementation review, fact-chain/status readback where the active carrier permits it, PR metadata/head binding, local canonicalization diagnostics validation, generation surface preservation, fixture-drift validation, aggregate fixture validation, hosted checks, scheduler-owned gate consumption, controlled merge, and closeout evidence remain required. recheck condition: require a minimal or full suite if the PR expands beyond `tools/check_demo_bootstrap_fixture.py`, the optional Makefile canonicalization alias, WI-1403 carriers, and PR metadata, or if it changes fixture contents, #1401 generation semantics, #1402 fixture drift/cleanliness split, #1404 docs closeout, generated runtime behavior, release/package behavior, permissions, or external-visible actions. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1403 validation-surface implementation, allowed WI-1403 carriers, and PR metadata. review requirement: current-head review or scheduler-authorized equivalent must consume the final canonicalization surface labels, evidence locators, generation preservation, fixture drift contract preservation, aggregate evidence, and PR metadata before merge-ready.
