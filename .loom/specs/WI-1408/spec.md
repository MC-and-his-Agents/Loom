# WI-1408 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1408 is a docs/evidence convergence Work Item for already implemented runtime regression validation surfaces from #1405, #1406, and #1407. It preserves the aggregate `loom-check-runtime-regression` entrypoint and records parent-consumable evidence without changing checker behavior, Makefile behavior, runtime semantics, shared contract/schema/parser vocabulary, release/package behavior, permissions, or external-visible actions. consumer boundary: this decision only skips formal suite artifacts; current-head implementation review, PR metadata/head binding, evidence-map and task-carrier readback, local focused surface validation, aggregate runtime regression validation, fact-chain/status/shadow evidence, hosted checks, scheduler-owned gate consumption, controlled merge after watcher merge_lane grant, no_release judgment, and later parent closeout consumption remain required. recheck condition: require a minimal or full suite if the PR expands beyond concise docs/evidence references and WI-1408 carriers, or if it changes `tools/check_loom_check_runtime_regressions.py`, Makefile command behavior, runtime fixtures, shared contract/schema/parser/failure vocabulary, release/package behavior, permissions, external-visible behavior, parent #1263/#1255 closeout, `.loom/reviews/**`, `.loom/status/current.md`, `.loom/shadow/**`, or merge execution without watcher grant. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1408 docs/evidence references, WI-1408 carriers, current status activation, scheduler review/shadow evidence, and the narrow WI-1407 locator correction required for accurate parent consumption. review requirement: scheduler-owned current-head review or gate must consume the final PR head, docs/evidence references, local validation evidence, PR metadata readback, hosted checks classification, and no_release rationale before merge-ready.
