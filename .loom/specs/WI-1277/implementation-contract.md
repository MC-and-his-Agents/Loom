# WI-1277 Implementation Contract

- Suite path: minimal

## Contract Surface

- `tools/loom_check.py` and shared runtime copies expose `--source-surface merge-gate`.
- `merge-gate` executes the existing source-self merge/PR/merge-ready gate fixture container under a stable named source surface and step name.
- `source-self-fixture` remains an aggregate and continues to include `review-run`, `merge-gate`, and the remaining source-self fixture checks.
- Generated skills runtime `loom_check.py` copies stay aligned with the canonical shared runtime generated by `python3 tools/loom.py skills generate --target . --apply --json`.

## Consumer Boundary

- Review and merge gates consume this contract through the focused `merge-gate` source surface, the aggregate `source-self-fixture` surface, generated skills parity, PR metadata, hosted checks, and WI-1277 carriers.
- #1278, #1279, #1280, and #1258 do not consume this contract until #1277 is merged and read back.

## Non-Goals

- Do not implement closeout-reconciliation, retire-workspace, installed-runtime, or parent closeout fixture surfaces in WI-1277.
- Do not change release, package, workflow, or user-visible merge behavior beyond exposing the named diagnostic source surface.
- Do not rename `review-run` or change the #1276 source-surface contract.

## Validation Binding

- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface merge-gate .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom_check.py --profile source --source-surface source-self-fixture .`
- `PYTHONDONTWRITEBYTECODE=1 python3 tools/loom.py skills check --target . --json`
