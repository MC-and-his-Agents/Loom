# WI-1401 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1401 is a narrow executable validation-surface split for `tools/check_demo_bootstrap_fixture.py`. It separates the isolated demo bootstrap generation command from the existing stable fixture drift comparison, adds generation-specific failure labels and evidence locators, and preserves the existing aggregate fixture check contract. A formal spec suite would duplicate the GitHub issue acceptance criteria without adding a new product or governance contract. consumer boundary: this decision only skips formal suite artifacts; current-head implementation review, fact-chain/status readback where the active carrier permits it, PR metadata/head binding, local generation-only validation, existing aggregate fixture validation, hosted checks, scheduler-owned gate consumption, controlled merge, and closeout evidence remain required. recheck condition: require a minimal or full suite if the PR expands beyond `tools/check_demo_bootstrap_fixture.py`, the optional Makefile generation alias, WI-1401 carriers, and PR metadata, or if it changes fixture contents, #1402 fixture drift/cleanliness semantics, #1403 canonicalization diagnostics, #1404 docs closeout, generated runtime behavior, release/package behavior, permissions, or external-visible actions. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1401 validation-surface implementation, allowed WI-1401 carriers, and PR metadata. review requirement: current-head review or scheduler-authorized equivalent must consume the final generation surface labels, evidence locators, fixture drift contract preservation, cleanliness evidence, and PR metadata before merge-ready.
