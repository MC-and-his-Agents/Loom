# WI-1269 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1269 is a narrow executable regression runner refactor that preserves the existing aggregate `tools/check_cli_contract.py` body and validates behavior through the script's own aggregate surface plus CLI selection smoke checks; a separate formal spec suite would duplicate the issue acceptance criteria without adding a new consumer contract. consumer boundary: suite validate, review, merge-ready, PR gate, hosted CI, and issue closeout consume this locator only as the formal suite decision; implementation review, fact-chain, PR head binding, local CLI contract validation, hosted checks, controlled merge, and closeout evidence remain required. recheck condition: require a full or minimal suite if this PR starts splitting additional regression surfaces, changing fixture semantics, altering aggregate CLI contract assertions, or touching #1270-#1274/#1276-#1280 scope. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1269 carriers and `tools/check_cli_contract.py`. review requirement: `.loom/reviews/WI-1269.json` must review PR #1334 head 494d7b83ec6d64f4e75c98838147b78bccc005bd before merge-ready.
