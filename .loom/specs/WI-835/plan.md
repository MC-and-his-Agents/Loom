# WI-835 Plan

1. Add the complex-existing authority migration playbook and evidence fixture.
2. Extend review execution, repo interop, controlled merge, and output-contract references.
3. Implement runtime payloads in `loom_flow.py` for the new schemas.
4. Add source-profile fixture validation in `loom_check.py`.
5. Regenerate `skills/` and demo runtime surfaces.
6. Validate with `skills_surface`, `py-compile`, `loom_check`, and `make check`.
7. Push PR #984, run merge gates, merge, and reconcile #836 through #842 plus parent #835.
