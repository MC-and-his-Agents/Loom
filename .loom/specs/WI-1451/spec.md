# WI-1451 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1451 is a bounded repository self-governance hardening slice that changes one pull-request workflow and the matching Loom carriers so the hosted check context becomes stable and always present. The governing evidence is the GitHub issue scope, the workflow diff, live host readback of current required contexts, the explicit `root-self-governance` candidate decision, local validation, PR metadata/head binding, hosted check readback, and scheduler-owned current-head review. A formal spec/plan/implementation-contract suite would restate that bounded governance contract without adding a new product/runtime contract. consumer boundary: this decision only skips formal suite artifacts; fact-chain/status carriers, current-head review, PR metadata, hosted checks, PR gate, live-config boundary/readback, no_release judgment, and closeout evidence remain required. recheck condition: require a minimal or full suite if the scope expands beyond `.github/workflows/node-installer-pr.yml` plus WI-1451 carriers, or if it changes controlled-merge product semantics, triggered-check aggregation, shared contract/schema/parser/failure vocabulary, branch-protection/ruleset mutation logic, runtime/package behavior, release behavior, permissions, external-visible actions, or other work items. scope proof: `git diff origin/main...HEAD` must stay limited to the node-installer PR workflow, WI-1451 carriers, current-item pointer refresh, shadow hash refresh, and PR metadata. review requirement: current-head review must consume the final workflow diff, the exact hosted check name observed on the PR head, the path-hit/path-miss behavior proof, the 2026-06-13 live required-context baseline, and the explicit `root-self-governance` candidate/live-config boundary decision before merge-ready.
