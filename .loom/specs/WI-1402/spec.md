# WI-1402 Suite Path Decision

- Suite path: not_applicable

- Formal-suite not_applicable: rationale: WI-1402 is a narrow executable validation-surface split for `tools/check_demo_bootstrap_fixture.py`. It keeps fixture tree drift as a named, targetable surface, adds an explicit `examples/new-project` tracked-cleanliness surface, and preserves the existing generation, canonicalization, fixture-drift, and aggregate fail-closed demo bootstrap validation contract. A formal spec suite would duplicate the GitHub issue acceptance criteria without adding a new product or governance contract. consumer boundary: this decision only skips formal suite artifacts; current-head implementation review, fact-chain/status readback where the active carrier permits it, PR metadata/head binding, local fixture-drift validation, local cleanliness validation, generation surface preservation, canonicalization diagnostics preservation, aggregate fixture validation, hosted checks, scheduler-owned gate consumption, controlled merge, and closeout evidence remain required. recheck condition: require a minimal or full suite if the PR expands beyond `tools/check_demo_bootstrap_fixture.py`, the optional Makefile fixture-drift/cleanliness aliases, WI-1402 carriers, and PR metadata, or if it changes fixture contents, #1401 generation semantics, #1403 canonicalization diagnostics, #1404 docs closeout, generated runtime behavior, release/package behavior, permissions, or external-visible actions. scope proof: `git diff origin/main...HEAD` must remain limited to WI-1402 validation-surface implementation, allowed WI-1402 carriers, and PR metadata. review requirement: current-head review or scheduler-authorized equivalent must consume the final fixture-drift labels, cleanliness labels, evidence locators, generation preservation, canonicalization preservation, aggregate evidence, and PR metadata before merge-ready.
