name: loom-check

on:
  push:
  pull_request:

jobs:
  loom-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run loom check
        run: make loom-check
