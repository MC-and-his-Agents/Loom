name: loom-pr-merge-gate

on:
  pull_request:
    branches:
      - main
  merge_group:

jobs:
  loom-pr-merge-gate:
    name: loom-pr-merge-gate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
          fetch-depth: 0

      - name: Check authored semantic review approval
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number || '' }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.event.merge_group.head_sha || github.sha }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref || github.event.merge_group.head_ref || github.ref_name }}
        run: |
          set -euo pipefail
          args=(pr-gate check --target . --head-sha "$PR_HEAD_SHA" --branch "$PR_HEAD_REF")
          if [ -n "$PR_NUMBER" ]; then
            args+=(--pr "$PR_NUMBER")
          fi
          python3 tools/loom_flow.py "${args[@]}"
